{
    "url": "/api/v1/numbers",
    "method": "GET",
    "response": {
        "iterate": "{{body.numbers}}",
        "output": {
            "label": "{{item.phone_number}}",
            "value": "{{item.id}}"
        }
    }
}
